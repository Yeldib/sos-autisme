{% extends 'admin/base.html.twig' %}

{% block title %}Modification du profil utilisateur n° {{proUser.id}}{% endblock %}

{% block body %}
	<div class="container">
		<h2 class="my-5 text-center">Modification du profil de {{proUser.fullName}}</h2>

        {{ form_start(form) }}

        <div class="container">
            <div class="row ">
                <div class="col-6 mx-auto">
                    <img src="{{ proUser.picture }}" alt="photo de {{proUser.fullName}}" class="avatar mb-2" >
                    {{ form_widget(form) }}
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-check"></i>
                            Enregistrer
                        </button>
                </div>
            </div>
        </div>
        {{ form_end(form) }}
	</div>
{% endblock %}
